@@ set
    domain      127.0.0.1:4000
    base_url    http://${domain}/

    home        tests/galen/home

Home page on desktop
    http://${domain} 1200x1080
        check ${home}/desktop.gspec --include "desktop"

Home page on tablet
    http://${domain} 768x1024
        check ${home}/tablet.gspec --include "mobile,all" --exclude "nomobile"

Home page on mobile
    http://${domain} 480x800
        check ${home}/mobile.gspec --include "mobile,all" --exclude "nomobile"

### galen check tests/galen/home/tablet.gspec --url localhost:4000 --size 480x800 --htmlreport tests/galen/reports/mobNew -Dphantomjs.binary.path=/usr/local/phantomjs/bin/phantomjs
