@@ set
    domain      127.0.0.1:4000
    base_url    http://${domain}/

Home page on desktop
    selenium phantomjs http://${domain} 1920x1080
        check tests/galen/home.gspec --include "desktop"

Home page on mobile
    selenium phantomjs http://${domain} 375x627
        check tests/galen/home.gspec --include "mobile" --exclude "nomobile"