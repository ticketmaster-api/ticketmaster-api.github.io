@objects
    body                    css     body
    header                  css     .top-bar
    base-content-wrapper    css     #api-explorer
    footer                  id      footer

= Support page content test =
    base-content-wrapper:
        % image file etalonImg/dumpsDesktop/objects/base-content-wrapper.png, error 2%

= Navigation test =
    header, footer:
        visible