@import tablet/tablet-header.gspec
@import tablet/tablet-content.gspec
@import tablet/tablet-header-menu.gspec
# keyword @import - not saw in local run test on Windows (becouse of '/' in path)

@objects
    base-content-wrapper    css     .base-content-wrapper

= Events image test (tablet) =
    base-content-wrapper:
        image file etalonImg/dumpsTablet/objects/base-content-wrapper.png, tolerance 40,  error 4%